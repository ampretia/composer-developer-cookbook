{
  "name": "photo-network",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "version": "which composer && composer --version",
    "build:BNA": "composer archive create --archiveFile photo-network.bna --sourceType dir --sourceName ./network/photo-network"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "composer-cli": "^0.18.0",
    "composer-playground": "^0.18.0",
    "composer-rest-server": "^0.18.0"
  },
  "dependencies": {
    "imghash": "0.0.3"
  }
}
